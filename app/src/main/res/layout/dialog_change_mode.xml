<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable name="dialog" type="ru.aleshi.letsplaycities.ui.network.ChangeModeDialog"/>
    </data>

    <LinearLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
    android:padding="10dp">

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="15dp"
                android:text="@string/dialog_change_mode_title"
                android:textSize="18sp"/>

        <RadioGroup
                android:onCheckedChanged="@{dialog::onModeChanged}"
                android:id="@+id/radioGroup"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>

        <TextView
                android:id="@+id/dcm_desc"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginBottom="10dp"
                android:text="@{dialog.description}"/>
    </LinearLayout>
</layout>